# Core framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Database
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# Settings / env
pydantic-settings==2.7.0
python-dotenv==1.0.1

# LLM
openai==1.57.4

# File handling
python-multipart==0.0.20   # multipart form / file upload
pypdf==4.3.1               # PDF text extraction (pure Python, no binary deps)

# Export
openpyxl==3.1.5             # Excel (.xlsx) export

# SSL — used only on Windows for corporate proxy (skipped on Linux/Railway)
truststore==0.10.4

# Rate limiting
slowapi==0.1.9

# HTTP client (used internally by openai SDK; explicit pin for stability)
httpx==0.28.1
